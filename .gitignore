# ── Private / local data ──────────────────────────────────────────────────
# These files contain personal portfolio data and must never be committed.

# Your actual target allocation (copy from claude/config.example.py to set up)
claude/config.py

# Runtime data generated by the app — stays on your laptop
app/app_config.json
app/trades_log.json

# Legacy scripts / snapshots
claude/portfolio_state.json
claude/portfolio_rebalancer.log
purchase_history.json

# Archives
claude.zip

# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.venv/
venv/

# ── macOS ─────────────────────────────────────────────────────────────────
.DS_Store
